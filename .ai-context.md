# bomi · 项目现状（.ai-context.md）

> 整合方维护。三端对话每次新会话**第一动作读取本文件**确认项目状态。
> 更新时机：阶段任务完成、技术决策落地、风险出现、架构演进。

## 项目概览

- **名称**：bomi
- **定位**：AI 食物拍照识别 + 饮食打卡 + 健康计划推荐
- **目标用户**：注重饮食健康的 C 端用户（小程序）+ 运营人员（管理后台）
- **启动日期**：2026-06-29

## 技术栈（已锁定）

| 层 | 技术栈 | 包路径 | 负责对话 |
|---|---|---|---|
| 共享层 | TypeScript | `packages/shared` | 整合方独占 |
| 小程序前端 | Uni-app + Vue3 + TS → mp-weixin | `packages/miniapp` | 对话① |
| 管理后台 | Vue3 + Vite + Element Plus | `packages/admin` | 对话② |
| 服务端 | NestJS + TS | `packages/server` | 对话③ |
| AI 调用层 | TypeScript（VLM 视觉模型） | `packages/ai` | 对话③ |
| 仓库结构 | Monorepo（pnpm workspace） | 根 | 整合方 |

## 当前阶段

**Stage 0 · 仓库初始化（已完成）**
- [x] Monorepo 骨架（5 包 + pnpm-workspace）
- [x] `packages/shared` 完整骨架（types/ + constants/）
- [x] `packages/{miniapp,admin,server,ai}` 包级占位
- [x] 根记忆文件 + docs/api-contract + docs/prompts
- [x] git 初始化 + 首次提交

**Stage 1 · 各端 CLI 初始化（待启动）**
- [ ] 对话①：Uni-app CLI 初始化 `packages/miniapp`
- [ ] 对话②：Vite 初始化 `packages/admin`
- [ ] 对话③：NestJS CLI 初始化 `packages/server` + ai 层骨架
- [ ] 各端接入 `@bomi/shared` workspace 依赖

## 三端进度看板

| 对话 | 包 | 当前状态 | 分支 |
|---|---|---|---|
| ① 小程序 | miniapp | 待 CLI 初始化 | - |
| ② 后台 | admin | 待 CLI 初始化 | - |
| ③ 服务端 | server + ai | 待 CLI 初始化 | - |

## 整合方独占写权限

以下文件/目录**仅整合方可写**，三端对话只读，需变更须提案：
- `packages/shared/**`
- `.ai-context.md` / `DECISIONS.md` / `.ai-memory.md` / `TECH_DEBT.md`
- `docs/api-contract.md`
- 根 `package.json` / `pnpm-workspace.yaml` / `.gitignore` / `.env.example` / 分支策略

## 跨端协同铁律

1. 接口字段变更：先改 `shared/types` → 同步 server DTO → 同步前端 api，三处同提交
2. 错误码新增：先改 `shared/constants/error-code.ts` → server 使用 → 前端处理
3. 业务状态变更：先改 `shared/constants/business.ts` → server DB/逻辑 → 前端展示
4. 前端禁直连 AI 供应商，统一经 server `/api/ai/*` 转发
5. API Key 仅 `packages/ai/config/env.ts` 从 `process.env` 读取，禁止硬编码、禁止入 shared
