# bomi 进行中任务 (.ai-memory.md)

> 破例占位：本文件原应由整合方维护。当前由 server 端补齐占位，待整合方接管。

## 当前任务：Stage 1 - 服务端 + AI 层脚手架（已完成）

### 进度
- [x] 加载 multi-terminal-dev-standard skill，读取 references/01 §2.7、07、08
- [x] 切出 feat/server-stage1 分支
- [x] 破例补齐最小骨架（根配置 + DECISIONS.md + 记忆文件 + .env.example）
- [x] 破例补齐最小 packages/shared（待整合方 review）
- [x] NestJS CLI 初始化 packages/server
- [x] 套用 §2.7 服务端架构补齐目录
- [x] 全局响应拦截器（BaseApiResponse<T>）
- [x] 全局异常过滤器（ERROR_CODE 转换）
- [x] JWT Guard + CurrentUser 装饰器
- [x] TraceId 全局中间件（修复异常路径无 traceId 问题）
- [x] AI 层 config/env.ts + constants.ts
- [x] AI 层 core/client.ts（OpenAI 兼容，recognizeFood/generatePlan/chat + SSE + retry）
- [x] AI 层 prompts/food-recognize.ts + plan-generate.ts
- [x] 自查：硬编码扫描（references/04）+ AI 自查（references/08 §11.13）+ Key 安全 5 条
- [x] 运行时冒烟测试：/api/health + 404 + traceId 链路全部通过
- [x] 输出改动文件清单 + shared 同步需求 + 前后端协同变更清单

### 冒烟测试结果（2026-06-29）
- `GET /api/health` → 200，响应体 `BaseApiResponse<HealthStatus>`：code=0/message="操作成功"/data={status:'ok',uptime,timestamp}/traceId/timestamp ✓
- `GET /api/nonexistent` → 404，响应体 `BaseApiResponse<null>`：code=40401/message="资源不存在"/data=null/traceId/timestamp ✓
- 响应头 `x-trace-id` 正常注入（成功 + 异常路径均注入）✓
- typecheck + build 全绿 ✓

### 待整合方确认事项（破例改动，待 review）
1. DECISIONS.md D003/D004/D005 内容是否准确
2. packages/shared 的最小类型/枚举定义是否可被整合方接管
3. 接口契约 docs/api-contract.md 是否已由整合方维护（当前缺失）
4. 是否同意 server 端破例补齐的骨架并入 main
5. 根 package.json / pnpm-workspace.yaml / tsconfig.base.json / .gitignore / .env.example 是否符合整合方预期

### 下阶段预告
- Stage 2：auth 模块（微信登录 + 手机号登录，D004 账号关联）完整实现 + JwtStrategy 注册到 auth 模块
- Stage 3：food 模块 + plan 模块 + AI 计费/限流
