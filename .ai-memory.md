# bomi · 进行中任务（.ai-memory.md）

> 整合方维护。记录当前进行中的任务与上下文，三端阶段任务完成后提示整合方更新。
> 完成的任务移到「已完成」段落，保留简短摘要。

## 当前进行中

### [整合方] Stage 1 启动准备
- **状态**：等待用户提供环境就绪信号
- **下一步**：用户完成 Node/pnpm/Git/微信开发者工具/AI Key/短信服务/数据库准备后，分发 Stage 1 任务卡给三端对话

## 待分发任务卡（Stage 1）

### 对话① 小程序前端
- 用 Uni-app CLI（`npx degit dcloudio/uni-preset-vue#vite-ts`）初始化 `packages/miniapp`
- 套用 references/01 的 2.2 Uni-app 架构
- 接入 `@bomi/shared` workspace 依赖
- 填充 `package.json` 的 `dev:mp-weixin` / `build:mp-weixin` / `typecheck` / `lint` 脚本
- 创建 `config/constants.ts` + `config/env.ts`（含小程序 AppID 占位、`useAiProxy: true`）
- 创建 `core/request.ts`（解包 `BaseApiResponse<T>`，code≠0 走异常）

### 对话② 管理后台
- 用 Vite 初始化 `packages/admin`（`npm create vite@latest`，选 vue-ts）
- 套用 references/01 的 2.6 后台管理架构
- 接入 `@bomi/shared` workspace 依赖 + Element Plus
- 填充 `package.json` 的 `dev` / `build` / `typecheck` / `lint` 脚本
- 创建 `config/` + `core/request.ts` + `router/` + `store/` + `layout/` 骨架

### 对话③ 服务端 + AI 层
- 用 NestJS CLI 初始化 `packages/server`（`nest new` 或手动搭骨架）
- 套用 references/01 的 2.7 服务端架构
- 接入 `@bomi/shared` + `@bomi/ai` workspace 依赖
- 填充 `package.json` 的 `start:dev` / `build` / `typecheck` / `lint` 脚本
- 创建全局响应拦截器（输出 `BaseApiResponse<T>`）+ 异常过滤器（用 `ERROR_CODE`）
- AI 层 `packages/ai`：创建 `config/env.ts`（从 process.env 读 Key）+ `core/client.ts` + `prompts/food-recognize.ts` 骨架

## 已完成

### [整合方] Stage 0 仓库初始化（2026-06-29）
- Monorepo 骨架（5 包 + pnpm-workspace）
- `packages/shared` 完整骨架（types/api/enum/user/food/plan/ai/ai-api + constants/error-code/business/ai）
- `packages/{miniapp,admin,server,ai}` 包级占位
- 根记忆文件 + `docs/api-contract.md` + `docs/prompts/{miniapp,admin,server}.md`
- git 初始化 + 首次提交
