{
  "name": "@bomi/ai",
  "version": "0.0.0",
  "private": true,
  "description": "bomi AI 调用层（食物识别 VLM + 计划推荐，被 server 调用）- 由对话③一并负责",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "build": "tsc"
  },
  "dependencies": {
    "@bomi/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
